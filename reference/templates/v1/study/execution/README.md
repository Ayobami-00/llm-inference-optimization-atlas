# Execution bundles

Add one directory per supported execution environment. Every bundle requires
`execution.yaml`, `README.md`, and `run.sh`; add lifecycle scripts only when the
declared resources require them.
