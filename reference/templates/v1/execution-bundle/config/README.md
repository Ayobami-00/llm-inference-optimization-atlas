# Bundle configuration

Place compact traces and configuration files here. Large, licensed, or generated inputs belong in the
shared Atlas cache and must be declared in `execution.yaml` with immutable identity and checksum.
