# External Sources

This registry contains passive metadata for authoritative external sources cited by Atlas artifacts. Contributors add a source only when introducing an externally grounded concept, specification, dataset, or method. Atlas experiments remain under `studies/`.

Source records never maintain reverse links. The graph compiler discovers citations and generates `Referenced by` indexes and BibTeX output.
