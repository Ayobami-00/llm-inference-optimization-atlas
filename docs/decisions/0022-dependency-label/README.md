# Dependency maintenance label

The repository declares a `dependencies` label because all three Dependabot
ecosystems request it in `.github/dependabot.yml`. The live repository label and
the checked-in catalog use the same name, color, and description so new update
pull requests do not emit missing-label warnings.
